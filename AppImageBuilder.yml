# appimage-builder recipe see https://appimage-builder.readthedocs.io for details
version: 1
AppDir:
  path: /home/natalie/game-projects/muffins-libadagio-cmake/AppDir
  app_info:
    id: muffin-mare
    name: Muffin Mare
    icon: application-vnd.appimage
    version: latest
    exec: usr/bin/muffins
    exec_args: $@
  files:
    include:
      - /lib64/ld-linux-x86-64.so.2
    exclude:
      - usr/share/man
      - usr/share/doc/*/README.*
      - usr/share/doc/*/changelog.*
      - usr/share/doc/*/NEWS.*
      - usr/share/doc/*/TODO.*
AppImage:
  arch: x86_64
  update-information: guess
